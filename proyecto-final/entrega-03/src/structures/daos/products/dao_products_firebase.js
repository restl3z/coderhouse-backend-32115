import Container_Firebase from '../../classes/container_firebase.js';

export default class DAO_Products_Firebase extends Container_Firebase {
  constructor(collection_name) {
    super(collection_name);
  }
}
